Вопросы 1-й Недели
1)Почему серверная часть использует TS(он же JS)?
В чём приимущества JS для сервера по сравнению с другими ЯП.
2)Можно ли на одном сервере держать несколько процессов(колезея), допустим с разными номерами портов?
Что бы можно было на одном VPS-е иметь несклько простеньких игр.
3) Не до конца понятен смысл(в знч. необходимость) дилегатов. То есть в каких местах они не заменимы?
К примеру в 6-м уроке вместо ForEach(ForEachEnemy), можно использовать это?:
for (var i = 0; i < state.players.Count; i++)
        {
            if (state.players[i] == _room.SessionId) { continue; }

            var positionEnemy = new Vector3(player.x - 200, 0f, player.y - 200) / 8;
            Instantiate(_enemy, positionEnemy, Quaternion.identity);
        }

4)Запущенный терминал с npm Start получается - это запущенный игровой сервер, который потребляет ресурсы ПК на котором мы,
на данный момент, тестируем мультиплеер?

Вопросы 2-й недели:
1)  При попадании пулей по врагу он начинает телепортироваться(только в момент попадания) по сцене, то возвращаться на примерное своё положение.
    Так же, при небольшом движении персонажем, на клиенте врага наш персонаж дёргается с большим интервалом.
    Возможно это связанно с тем, что отсылая каждый кадр свою позицию, сервер либо пропускает обновление своих переменных,
    либо не отправляет пакет "вражескому" клиенту и/или из-за редкости пришедших данных средняя скорость для врага получается большой.

2)  Сделал раскладку действий(прыжок, присед и т.д.) клавиатуры в PlayerController через KeyCode-ы, думаю сделать ScriptableObject
    со списком KeyCode-ов для подобного, такой вариант нормальный или есть лучше система для сохранения/изменения и использования
    раскладки клавиш?

3)  Сделал приседания через анимацию(CharacterAnimation  _animatorBody.SetBool()). Но если отпустить и резко нажать кнопку приседания,
    то анимация начнётся с начала, есть способ, что бы анимация не начиналась с начала, а продолжилась на том месте, где она находился
    до повторного нажатия приседания? Или лучше делать другим способом анимацию приседания?

4)  Закрытие клиента у врага на нашем клиенте возникает ошибка, что _player в EnemyController пустой(NullReferenceException),
    если сделать проверку на null, то даже так не получается удалить объект из-за "Destroying assets is not permitted to avoid data loss."
    Что за данные он боится потерять?
   
